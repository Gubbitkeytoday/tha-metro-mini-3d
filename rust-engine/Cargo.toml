[workspace]
resolver = "3"
members = ["sim-core", "wasm", "preprocessor"]

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
bincode = { version = "2", features = ["serde"] }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
